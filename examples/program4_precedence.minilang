// Program 4: Operator precedence stress test
// Expected: with no input -> prints correct precedence results
a = 2 + 3 * 4;        // 14
print a;
b = 10 - 2 * 3;       // 4
print b;
c = 20 / 4 + 1;       // 6
print c;
d = (2 + 3) * 4;      // 20
print d;
e = 10 == 5 + 5;      // 1 (true)
print e;
f = 3 < 2 + 2;        // 1 (true)
print f;
end
